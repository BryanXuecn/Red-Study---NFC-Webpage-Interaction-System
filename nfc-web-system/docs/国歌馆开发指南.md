# 国歌馆开发指南 - "觉醒之声"

## 📋 开发任务概述

### 模块信息
- **模块名称**: 02-国歌馆-觉醒之声
- **开发状态**: 🚧 待开发
- **预估工期**: 5-7天
- **技术难度**: ⭐⭐⭐⭐ (中高难度)

### 核心功能需求
1. **音频可视化展示** - 实时波形动画
2. **国歌歌词交互** - 逐句高亮显示
3. **历史故事展示** - 国歌创作背景
4. **交互式体验** - 音频播放控制

## 🎨 设计规范参考

### 基础样式继承宣言馆
```css
/* 继承现代化CSS变量系统 */
@import url('../../静态资源/common-css/variables.css');

/* 国歌馆主题色扩展 */
:root {
  --anthem-blue: #1e40af;      /* 国歌蓝 */
  --anthem-gold: var(--accent-gold); /* 继承金色 */
  --anthem-red: var(--primary-red);  /* 继承红色 */
  --wave-color: #3b82f6;       /* 波形蓝色 */
}
```

### 布局结构建议
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>觉醒之声 - 《义勇军进行曲》展示馆</title>
    
    <!-- 继承通用样式 -->
    <link rel="stylesheet" href="../../静态资源/common-css/reset.css">
    <link rel="stylesheet" href="../../静态资源/common-css/variables.css">
    <link rel="stylesheet" href="../../静态资源/common-css/responsive.css">
    <link rel="stylesheet" href="../css/anthem-hall.css?v=20250821">
</head>
<body>
    <!-- 加载动画 -->
    <div id="loading-screen" class="loading-screen">
        <div class="audio-wave-loading">
            <div class="wave-bar" style="--delay: 0s"></div>
            <div class="wave-bar" style="--delay: 0.1s"></div>
            <div class="wave-bar" style="--delay: 0.2s"></div>
            <div class="wave-bar" style="--delay: 0.3s"></div>
            <div class="wave-bar" style="--delay: 0.4s"></div>
        </div>
        <h2 class="loading-title">义勇军进行曲展示馆</h2>
        <p class="loading-text">聆听时代强音 · 感受民族觉醒</p>
    </div>

    <!-- 主容器 -->
    <div id="main-container" class="main-container hidden">
        <!-- 头部导航 -->
        <header class="header">
            <div class="header-content">
                <div class="hall-badge">
                    <div class="badge-icon">
                        <div class="chinese-seal">歌</div>
                    </div>
                    <div class="hall-info">
                        <h1 class="hall-name">《义勇军进行曲》展示馆</h1>
                        <p class="hall-subtitle">March of the Volunteers</p>
                    </div>
                </div>
                <div class="progress-indicator">
                    <div class="progress-circle">
                        <span class="step-number">2</span>
                    </div>
                    <span class="step-label">第二站</span>
                </div>
            </div>
        </header>

        <!-- 主要内容区域 -->
        <main class="main-content">
            <!-- NFC 访问成功指示器 -->
            <div id="nfc-status" class="nfc-status">
                <div class="nfc-icon-container">
                    <div class="nfc-icon">
                        <div class="nfc-symbol"></div>
                        <div class="nfc-waves">
                            <div class="wave"></div>
                            <div class="wave"></div>
                            <div class="wave"></div>
                        </div>
                    </div>
                </div>
                <div class="nfc-instruction">
                    <h3 class="nfc-title">NFC 访问成功</h3>
                    <p class="nfc-text">您已通过NFC芯片成功访问本页面</p>
                </div>
            </div>

            <!-- 音频可视化区域 -->
            <section class="audio-visualization-section">
                <div class="visualization-container">
                    <div class="album-cover">
                        <div class="vinyl-record">
                            <div class="record-center"></div>
                            <div class="record-grooves"></div>
                        </div>
                        <div class="album-info">
                            <h2 class="song-title">义勇军进行曲</h2>
                            <p class="composer">词：田汉 · 曲：聂耳</p>
                            <p class="year">1935年</p>
                        </div>
                    </div>
                    
                    <!-- 音频波形显示 -->
                    <div class="audio-waveform">
                        <canvas id="waveform-canvas" width="800" height="200"></canvas>
                        <div class="frequency-bars">
                            <!-- 由JS生成频率柱 -->
                        </div>
                    </div>
                </div>
            </section>

            <!-- 歌词交互区域 -->
            <section class="lyrics-section">
                <div class="lyrics-container">
                    <h3 class="section-title">歌词展示</h3>
                    <div class="lyrics-scroll">
                        <div class="lyrics-line" data-time="0">
                            <span class="chinese">起来！不愿做奴隶的人们！</span>
                            <span class="pinyin">qǐ lái! bù yuàn zuò nú lì de rén men!</span>
                        </div>
                        <div class="lyrics-line" data-time="3.5">
                            <span class="chinese">把我们的血肉，筑成我们新的长城！</span>
                            <span class="pinyin">bǎ wǒ men de xuè ròu, zhù chéng wǒ men xīn de cháng chéng!</span>
                        </div>
                        <!-- 更多歌词行... -->
                    </div>
                </div>
            </section>

            <!-- 历史故事区域 -->
            <section class="history-section">
                <div class="story-container">
                    <div class="composer-portrait">
                        <div class="portrait-frame">
                            <div class="placeholder-portrait">
                                <div class="portrait-text">聂耳</div>
                            </div>
                        </div>
                        <div class="composer-info">
                            <h4>聂耳 (1912-1935)</h4>
                            <p>人民音乐家</p>
                        </div>
                    </div>
                    
                    <div class="story-content">
                        <h3 class="story-title">觉醒的号角</h3>
                        <div class="story-text">
                            <p>1935年，中华民族正处于生死存亡的关键时刻。年仅23岁的聂耳...</p>
                        </div>
                    </div>
                </div>
            </section>

            <!-- 音频播放控制区域 -->
            <section class="audio-controls-section">
                <div class="player-container">
                    <div class="playback-controls">
                        <button id="play-pause-btn" class="play-pause-btn">
                            <svg class="play-icon" viewBox="0 0 24 24">
                                <path d="M8 5v14l11-7z"/>
                            </svg>
                            <svg class="pause-icon hidden" viewBox="0 0 24 24">
                                <path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"/>
                            </svg>
                        </button>
                        
                        <div class="progress-container">
                            <div class="progress-bar">
                                <div id="progress-fill" class="progress-fill"></div>
                            </div>
                            <div class="time-display">
                                <span id="current-time">0:00</span>
                                <span>/</span>
                                <span id="duration">2:46</span>
                            </div>
                        </div>
                        
                        <button id="volume-btn" class="volume-btn">
                            <svg viewBox="0 0 24 24">
                                <path d="M3 9v6h4l5 5V4L7 9H3zm13.5 3c0-1.77-1.02-3.29-2.5-4.03v8.05c1.48-.73 2.5-2.25 2.5-4.02z"/>
                            </svg>
                        </button>
                    </div>
                    
                    <!-- 隐藏的 audio 元素 -->
                    <audio id="anthem-audio" preload="none">
                        <source src="../assets/audio/march-of-volunteers.mp3" type="audio/mpeg">
                        <source src="../assets/audio/march-of-volunteers.ogg" type="audio/ogg">
                        音频文件暂未配置。
                    </audio>
                </div>
            </section>
        </main>

        <!-- 底部导航 -->
        <footer class="footer">
            <div class="completion-status">
                <div class="stamp-slot completed">
                    <div class="stamp-icon">🎵</div>
                    <span class="stamp-label">觉醒之章</span>
                </div>
            </div>
            
            <div class="navigation-buttons">
                <button id="prev-hall-btn" class="nav-btn prev-btn">
                    <svg viewBox="0 0 24 24" class="arrow-icon">
                        <path d="M12 20l1.41-1.41L7.83 13H20v-2H7.83l5.58-5.59L12 4l-8 8z"/>
                    </svg>
                    <span>返回宣言馆</span>
                </button>
                
                <button id="next-hall-btn" class="nav-btn next-btn">
                    <span>前往滨江馆</span>
                    <svg viewBox="0 0 24 24" class="arrow-icon">
                        <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                    </svg>
                </button>
            </div>
        </footer>
    </div>

    <!-- JavaScript 引用 -->
    <script src="../../静态资源/common-js/polyfills.js"></script>
    <script src="../../静态资源/common-js/config.js"></script>
    <script src="../../共通组件/nfc-reader/nfcHandler.js"></script>
    <script src="../../共通组件/ui-components/audioPlayer.js"></script>
    <script src="../js/anthem-hall.js"></script>
</body>
</html>
```

## 🎵 核心功能实现指导

### 1. 音频可视化实现
```javascript
class AudioVisualizer {
    constructor(audioElement, canvasElement) {
        this.audio = audioElement;
        this.canvas = canvasElement;
        this.ctx = this.canvas.getContext('2d');
        this.audioContext = null;
        this.analyzer = null;
        this.dataArray = null;
        this.animationId = null;
        
        this.initAudioContext();
    }
    
    initAudioContext() {
        // 创建音频上下文
        this.audioContext = new (window.AudioContext || window.webkitAudioContext)();
        this.analyzer = this.audioContext.createAnalyser();
        this.analyzer.fftSize = 256;
        
        // 连接音频源
        const source = this.audioContext.createMediaElementSource(this.audio);
        source.connect(this.analyzer);
        this.analyzer.connect(this.audioContext.destination);
        
        this.dataArray = new Uint8Array(this.analyzer.frequencyBinCount);
    }
    
    startVisualization() {
        const draw = () => {
            this.analyzer.getByteFrequencyData(this.dataArray);
            this.drawWaveform();
            this.animationId = requestAnimationFrame(draw);
        };
        draw();
    }
    
    drawWaveform() {
        const { width, height } = this.canvas;
        this.ctx.clearRect(0, 0, width, height);
        
        const barWidth = width / this.dataArray.length;
        let x = 0;
        
        for (let i = 0; i < this.dataArray.length; i++) {
            const barHeight = (this.dataArray[i] / 255) * height;
            
            // 渐变色彩
            const gradient = this.ctx.createLinearGradient(0, height, 0, height - barHeight);
            gradient.addColorStop(0, '#dc2626');
            gradient.addColorStop(0.5, '#fbbf24');
            gradient.addColorStop(1, '#1e40af');
            
            this.ctx.fillStyle = gradient;
            this.ctx.fillRect(x, height - barHeight, barWidth - 2, barHeight);
            
            x += barWidth;
        }
    }
    
    stopVisualization() {
        if (this.animationId) {
            cancelAnimationFrame(this.animationId);
        }
    }
}
```

### 2. 歌词同步显示
```javascript
class LyricsSync {
    constructor() {
        this.lyrics = [
            { time: 0, text: "起来！不愿做奴隶的人们！", pinyin: "qǐ lái! bù yuàn zuò nú lì de rén men!" },
            { time: 3.5, text: "把我们的血肉，筑成我们新的长城！", pinyin: "bǎ wǒ men de xuè ròu, zhù chéng wǒ men xīn de cháng chéng!" },
            { time: 8.0, text: "中华民族到了最危险的时候，", pinyin: "zhōng huá mín zú dào le zuì wēi xiǎn de shí hou," },
            { time: 12.5, text: "每个人被迫着发出最后的吼声。", pinyin: "měi gè rén bèi pò zhe fā chū zuì hòu de hǒu shēng." },
            { time: 17.0, text: "起来！起来！起来！", pinyin: "qǐ lái! qǐ lái! qǐ lái!" },
            { time: 20.0, text: "我们万众一心，", pinyin: "wǒ men wàn zhòng yī xīn," },
            { time: 22.5, text: "冒着敌人的炮火，前进！", pinyin: "mào zhe dí rén de pào huǒ, qián jìn!" },
            { time: 26.0, text: "冒着敌人的炮火，前进！", pinyin: "mào zhe dí rén de pào huǒ, qián jìn!" },
            { time: 29.5, text: "前进！前进！进！", pinyin: "qián jìn! qián jìn! jìn!" }
        ];
        
        this.currentLineIndex = -1;
        this.lyricsContainer = document.querySelector('.lyrics-scroll');
        this.createLyricsElements();
    }
    
    createLyricsElements() {
        this.lyricsContainer.innerHTML = '';
        
        this.lyrics.forEach((line, index) => {
            const lineElement = document.createElement('div');
            lineElement.className = 'lyrics-line';
            lineElement.dataset.index = index;
            
            lineElement.innerHTML = `
                <span class="chinese">${line.text}</span>
                <span class="pinyin">${line.pinyin}</span>
            `;
            
            this.lyricsContainer.appendChild(lineElement);
        });
    }
    
    updateCurrentLine(currentTime) {
        // 找到当前应该高亮的歌词行
        let newIndex = -1;
        for (let i = this.lyrics.length - 1; i >= 0; i--) {
            if (currentTime >= this.lyrics[i].time) {
                newIndex = i;
                break;
            }
        }
        
        if (newIndex !== this.currentLineIndex) {
            // 移除之前的高亮
            if (this.currentLineIndex >= 0) {
                const prevLine = this.lyricsContainer.querySelector(`[data-index="${this.currentLineIndex}"]`);
                if (prevLine) {
                    prevLine.classList.remove('active');
                }
            }
            
            // 添加新的高亮
            if (newIndex >= 0) {
                const currentLine = this.lyricsContainer.querySelector(`[data-index="${newIndex}"]`);
                if (currentLine) {
                    currentLine.classList.add('active');
                    // 滚动到当前行
                    currentLine.scrollIntoView({ behavior: 'smooth', block: 'center' });
                }
            }
            
            this.currentLineIndex = newIndex;
        }
    }
}
```

### 3. 主控制类
```javascript
class AnthemHall {
    constructor() {
        this.audioElement = null;
        this.visualizer = null;
        this.lyricsSync = null;
        this.isPlaying = false;
        
        this.init();
    }
    
    async init() {
        // 等待DOM加载完成
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', () => this.initializeComponents());
        } else {
            this.initializeComponents();
        }
    }
    
    initializeComponents() {
        // 初始化DOM元素
        this.audioElement = document.getElementById('anthem-audio');
        const canvas = document.getElementById('waveform-canvas');
        
        // 初始化组件
        this.visualizer = new AudioVisualizer(this.audioElement, canvas);
        this.lyricsSync = new LyricsSync();
        
        // 绑定事件
        this.bindEvents();
        
        // 隐藏加载屏幕
        setTimeout(() => {
            document.getElementById('loading-screen').style.opacity = '0';
            setTimeout(() => {
                document.getElementById('loading-screen').style.display = 'none';
                document.getElementById('main-container').classList.remove('hidden');
            }, 500);
        }, 2000);
    }
    
    bindEvents() {
        const playPauseBtn = document.getElementById('play-pause-btn');
        const progressBar = document.querySelector('.progress-bar');
        
        // 播放/暂停按钮
        playPauseBtn.addEventListener('click', () => {
            if (this.isPlaying) {
                this.pauseAudio();
            } else {
                this.playAudio();
            }
        });
        
        // 进度条点击
        progressBar.addEventListener('click', (e) => {
            const rect = progressBar.getBoundingClientRect();
            const percent = (e.clientX - rect.left) / rect.width;
            this.audioElement.currentTime = percent * this.audioElement.duration;
        });
        
        // 音频事件
        this.audioElement.addEventListener('timeupdate', () => {
            this.updateProgress();
            this.lyricsSync.updateCurrentLine(this.audioElement.currentTime);
        });
        
        this.audioElement.addEventListener('ended', () => {
            this.pauseAudio();
        });
    }
    
    async playAudio() {
        try {
            // 恢复音频上下文（用户手势后）
            if (this.visualizer.audioContext.state === 'suspended') {
                await this.visualizer.audioContext.resume();
            }
            
            await this.audioElement.play();
            this.isPlaying = true;
            this.updatePlayButton();
            this.visualizer.startVisualization();
            this.startVinylRotation();
            
        } catch (error) {
            console.error('音频播放失败:', error);
            this.showMessage('音频播放失败，请检查设备设置', 'error');
        }
    }
    
    pauseAudio() {
        this.audioElement.pause();
        this.isPlaying = false;
        this.updatePlayButton();
        this.visualizer.stopVisualization();
        this.stopVinylRotation();
    }
    
    updatePlayButton() {
        const playIcon = document.querySelector('.play-icon');
        const pauseIcon = document.querySelector('.pause-icon');
        
        if (this.isPlaying) {
            playIcon.classList.add('hidden');
            pauseIcon.classList.remove('hidden');
        } else {
            playIcon.classList.remove('hidden');
            pauseIcon.classList.add('hidden');
        }
    }
    
    updateProgress() {
        const progress = (this.audioElement.currentTime / this.audioElement.duration) * 100;
        document.getElementById('progress-fill').style.width = `${progress}%`;
        
        // 更新时间显示
        document.getElementById('current-time').textContent = this.formatTime(this.audioElement.currentTime);
    }
    
    formatTime(seconds) {
        const mins = Math.floor(seconds / 60);
        const secs = Math.floor(seconds % 60);
        return `${mins}:${secs.toString().padStart(2, '0')}`;
    }
    
    startVinylRotation() {
        const vinyl = document.querySelector('.vinyl-record');
        if (vinyl) {
            vinyl.style.animationPlayState = 'running';
        }
    }
    
    stopVinylRotation() {
        const vinyl = document.querySelector('.vinyl-record');
        if (vinyl) {
            vinyl.style.animationPlayState = 'paused';
        }
    }
    
    showMessage(text, type = 'info') {
        // 实现消息提示功能
        console.log(`${type.toUpperCase()}: ${text}`);
    }
}

// 初始化应用
window.anthemHall = new AnthemHall();
```

## 🎨 样式实现要点

### 加载动画 - 音频波形效果
```css
.audio-wave-loading {
    display: flex;
    align-items: end;
    gap: 4px;
    height: 60px;
}

.wave-bar {
    width: 6px;
    background: linear-gradient(to top, var(--primary-red), var(--accent-gold), var(--anthem-blue));
    border-radius: 3px;
    animation: waveAnimation 1.5s ease-in-out infinite;
    animation-delay: var(--delay);
}

@keyframes waveAnimation {
    0%, 100% { height: 20px; }
    50% { height: 60px; }
}
```

### 黑胶唱片动画
```css
.vinyl-record {
    width: 200px;
    height: 200px;
    background: radial-gradient(
        circle at center,
        #1a1a1a 30%,
        #2a2a2a 31%,
        #1a1a1a 32%,
        #2a2a2a 33%,
        #1a1a1a 60%,
        #333 100%
    );
    border-radius: 50%;
    position: relative;
    animation: vinylSpin 2s linear infinite;
    animation-play-state: paused;
}

.record-center {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 30px;
    height: 30px;
    background: var(--accent-gold);
    border-radius: 50%;
    box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.3);
}

@keyframes vinylSpin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}
```

### 歌词高亮效果
```css
.lyrics-line {
    padding: 1rem;
    margin: 0.5rem 0;
    border-radius: 0.5rem;
    transition: all 0.3s ease;
    opacity: 0.6;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
}

.lyrics-line.active {
    background: var(--bg-glass);
    border: 1px solid var(--accent-gold);
    opacity: 1;
    transform: scale(1.02);
    box-shadow: var(--shadow-medium);
}

.lyrics-line .chinese {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--text-light);
}

.lyrics-line .pinyin {
    font-size: 0.875rem;
    color: var(--text-gray);
    font-style: italic;
}
```

## 📂 文件结构
```
02-国歌馆-觉醒之声/
├── assets/
│   ├── audio/
│   │   ├── march-of-volunteers.mp3    # 国歌音频文件
│   │   └── march-of-volunteers.ogg    # 备用格式
│   └── images/
│       └── nie-er-portrait.jpg        # 聂耳肖像（可选）
├── css/
│   └── anthem-hall.css                # 主样式文件
├── html/
│   └── index.html                     # 主页面
└── js/
    └── anthem-hall.js                 # 主JavaScript文件
```

## 🔧 开发步骤建议

### 第1天：项目结构搭建
1. 创建文件夹结构
2. 复制宣言馆的基础模板
3. 修改HTML结构和CSS变量
4. 实现基础页面布局

### 第2-3天：音频功能开发
1. 实现音频播放控制
2. 开发可视化组件
3. 测试音频兼容性

### 第4-5天：歌词同步功能
1. 实现歌词数据结构
2. 开发时间同步逻辑
3. 完善用户交互

### 第6-7天：优化和测试
1. 移动端适配优化
2. 性能优化和错误处理
3. 全面测试和调试

## 📱 移动端适配重点

```css
@media (max-width: 768px) {
    .vinyl-record {
        width: 150px;
        height: 150px;
    }
    
    .lyrics-line .chinese {
        font-size: 1.125rem;
    }
    
    .audio-waveform canvas {
        width: 100%;
        height: 120px;
    }
}

@media (max-width: 480px) {
    .vinyl-record {
        width: 120px;
        height: 120px;
    }
    
    .lyrics-container {
        padding: 1rem;
    }
}
```

## 🐛 常见问题预防

1. **音频自动播放限制**：需要用户手势触发
2. **音频上下文状态**：检查和恢复suspended状态
3. **Canvas高分辨率**：考虑设备像素比
4. **内存泄漏**：及时清理音频上下文和动画

## 📝 开发备注

- 继承宣言馆的现代化设计风格
- 保持与项目整体的一致性
- 重点关注音频体验的流畅性
- 确保移动端的触摸友好性

---

*此文档为国歌馆开发的详细指导，请参考宣言馆的实际实现经验进行开发。*